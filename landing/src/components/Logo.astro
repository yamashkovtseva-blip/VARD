---
interface Props {
  size?: number;
  class?: string;
}
const { size = 28, class: className = '' } = Astro.props;
const base = import.meta.env.BASE_URL;
---

<img
  src={`${base}logo.png`}
  alt="Vard"
  width={size}
  height={size}
  class={className}
  loading="eager"
  decoding="sync"
/>
