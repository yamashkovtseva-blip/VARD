---
interface Props {
  size?: number;
  class?: string;
}
const { size = 28, class: className = '' } = Astro.props;
---

<img
  src="/logo.png"
  alt="Vard"
  width={size}
  height={size}
  class={className}
  loading="eager"
  decoding="sync"
/>
